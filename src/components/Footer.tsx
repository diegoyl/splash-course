import React from "react";

const Footer: React.FC = () => {
  return (
    // just adds some empty space at the bottom of pages for scrolling
    // might add some links or soemthing
    <footer>
    </footer>
  );
};

export default Footer;


